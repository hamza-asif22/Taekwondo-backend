# 🥋 Taekwondo Pose Analysis API

FastAPI backend for detecting & analyzing Taekwondo stances using Mediapipe + ML.

## 🚀 Run Locally
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
